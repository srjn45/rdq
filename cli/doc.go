// Command rdq is the rdq ops CLI: queue stats, DLQ browse/inspect/redrive/purge,
// and schema migrations — with two transports: API mode (--server URL) and
// direct-storage mode (--dsn postgres://…).
package main
